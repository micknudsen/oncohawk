/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    oncohawk Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

params {

    // Workflow selection
    workflow                   = 'oncohawk'

    // Input / output
    input                      = null
    outdir                     = 'results'
    publish_dir_mode           = 'copy'

    // Reference genome
    reference_dir              = 'references'
    genome_fasta               = null   // Set after PREPARE_REFERENCE, or provide directly
    genome_url                 = 'https://ftp.ncbi.nlm.nih.gov/genomes/all/GCA/000/001/405/GCA_000001405.15_GRCh38/seqs_for_alignment_pipelines.ucsc_ids/GCA_000001405.15_GRCh38_no_alt_analysis_set.fna.gz'


    // Boilerplate
    help                       = false
    version                    = false

}

// Load base resource configuration
includeConfig 'conf/base.config'

// Load module-level publishDir and resource overrides
includeConfig 'conf/modules.config'

// Profiles
profiles {

    // ── Container runtimes ──────────────────────────────────────────────────
    docker {
        docker.enabled         = true
        docker.runOptions      = '-u $(id -u):$(id -g)'
        singularity.enabled    = false
    }

    singularity {
        singularity.enabled    = true
        singularity.autoMounts = true
        docker.enabled         = false
    }

    // ── Execution environments ───────────────────────────────────────────────
    test {
        includeConfig 'conf/test.config'
    }

    hpc {
        includeConfig 'conf/hpc.config'
    }

    // ── Debug ────────────────────────────────────────────────────────────────
    debug {
        dumpHashes              = true
        process.beforeScript    = 'echo $HOSTNAME'
        cleanup                 = false
    }

}

// Shell settings for all processes
process.shell = ['/bin/bash', '-euo', 'pipefail']

// Reporting
timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_timeline.html"
    overwrite = true
}

report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_report.html"
    overwrite = true
}

trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_trace.txt"
    overwrite = true
}

dag {
    enabled = true
    file    = "${params.outdir}/pipeline_info/pipeline_dag.html"
    overwrite = true
}

// Pipeline manifest
manifest {
    name            = 'oncohawk'
    author          = 'Michael Knudsen'
    description     = 'Nextflow-based pipeline for tumor-only inference of somatic variation in myeloid malignancies'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=24.04.0'
    version         = '0.1.0'
}
