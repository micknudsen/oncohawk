/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    oncohawk modules configuration
    Per-process publishDir settings and resource overrides
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

process {

    if (params.workflow == 'prepare_reference') {
        // ── PREPARE_REFERENCE outputs go to the reference directory ────────────
        withName: 'PREPARE_REFERENCE:WGET_GENOME' {
            publishDir = [
                path: { "${params.reference_dir}" },
                mode: params.publish_dir_mode,
                saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
            ]
        }

        withName: 'PREPARE_REFERENCE:BWAMEM2_INDEX' {
            publishDir = [
                path: { "${params.reference_dir}" },
                mode: params.publish_dir_mode,
                saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
            ]
        }
    }

    if (params.workflow == 'oncohawk') {
        // ── ONCOHAWK outputs go to the results directory ────────────────────────

        withName: 'ONCOHAWK:CUTADAPT_BWAMEM2_MEM' {
            publishDir = [
                path: { "${params.outdir}/qc/cutadapt" },
                mode: params.publish_dir_mode,
                saveAs: { filename ->
                    if (filename.equals('versions.yml')) return null
                    if (filename.endsWith('.cutadapt.log')) return filename
                    return null
                }
            ]
        }

        withName: 'ONCOHAWK:SAMBAMBA_MARKDUP' {
            publishDir = [
                path: { "${params.outdir}" },
                mode: params.publish_dir_mode,
                saveAs: { filename ->
                    if (filename.equals('versions.yml')) return null
                    if (filename.endsWith('.markdup.log')) return "qc/markdup/${filename}"
                    if (filename.endsWith('.bam') || filename.endsWith('.bam.bai')) return "alignment/${filename}"
                    return null
                }
            ]
        }

        withName: 'ONCOHAWK:SAMTOOLS_MERGE' {
            publishDir = [
                path: { "${params.outdir}/alignment" },
                mode: params.publish_dir_mode,
                saveAs: { _filename -> null }
            ]
        }
    }

}


